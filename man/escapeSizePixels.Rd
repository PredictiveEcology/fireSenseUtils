% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objFunSpread.R
\name{escapeSizePixels}
\alias{escapeSizePixels}
\title{Pixels a fire must burn to count as escaped}
\usage{
escapeSizePixels(escapeSizeHa, landscape)
}
\arguments{
\item{escapeSizeHa}{Size (ha) a fire must reach to count as escaped.}

\item{landscape}{A \code{SpatRaster} (or anything with \code{\link[terra:res]{terra::res()}}) in metres.}
}
\value{
An integer, at least 1.
}
\description{
The smallest whole number of pixels whose area is at least \code{escapeSizeHa}. Shared by the spread
fit (\code{\link[=.objfunSpreadFit]{.objfunSpreadFit()}}) and fireSense's burning, so "escaped" means the same in both.
}
\examples{
r <- terra::rast(nrows = 10, ncols = 10, xmin = 0, xmax = 2400, ymin = 0, ymax = 2400)
escapeSizePixels(50, r) # 240 m pixels are 5.76 ha: 9 pixels
}
