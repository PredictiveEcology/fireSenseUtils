% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getFirePolygons.R
\name{latestNBACUrl}
\alias{latestNBACUrl}
\title{Discover the URL of the most recent NBAC shapefile archive}
\usage{
latestNBACUrl(
  base = "https://cwfis.cfs.nrcan.gc.ca/downloads/nbac/",
  fallback = paste0(base, "NBAC_1972to2025_20260513_shp.zip")
)
}
\arguments{
\item{base}{Character. Directory listing URL (must be the parent folder
that serves the autoindex page).}

\item{fallback}{Character. URL to return when discovery fails.}
}
\value{
A single character URL.
}
\description{
Parses the Apache directory listing at \code{base} and returns the URL of the
newest file matching \verb{NBAC_<startYear>to<endYear>_<YYYYMMDD>_shp.zip},
selected by the YYYYMMDD stamp embedded in the filename. If the listing
cannot be read or no matching file is found, \code{fallback} is returned, so
callers can rely on getting \emph{some} URL back.
}
