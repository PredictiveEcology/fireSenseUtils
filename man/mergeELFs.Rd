% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ELFmerge.R
\name{mergeELFs}
\alias{mergeELFs}
\title{Merge ELF maps as planned}
\usage{
mergeELFs(ELFs, plan)
}
\arguments{
\item{ELFs}{list with \code{rasWhole} and \code{rasCentered} (named lists of \code{SpatRaster}) and optionally
\code{poly}, as returned by \code{\link[=makeELFs]{makeELFs()}}.}

\item{plan}{\code{data.table} from \code{\link[=ELFmergePlan]{ELFmergePlan()}}.}
}
\value{
\code{ELFs}, with each merge's members replaced by the merged ELF.
}
\description{
Replaces the members of each merge in \code{\link[=ELFmergePlan]{ELFmergePlan()}} by one ELF. Its \code{rasWhole} layer is the
cell-by-cell maximum of the members' layers, so a cell that is core in any member is core. A
buffer drawn around the union of cores equals the union of the members' buffers, so the buffer
does not need redrawing. Its \code{rasCentered} layer is that map projected onto a Lambert conformal
conic centred on the merged core, as \code{\link[=makeELFs]{makeELFs()}} centres each ELF. Its \code{poly} rows are rebuilt
from the merged layer the way \code{\link[=makeELFs]{makeELFs()}} builds them.
}
