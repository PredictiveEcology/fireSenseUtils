% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{prepare_FuelCovsCoarse}
\alias{prepare_FuelCovsCoarse}
\title{Create and aggregate fuel covariate rasters to a coarse resolution}
\usage{
prepare_FuelCovsCoarse(..., rasTemplate, fact)
}
\arguments{
\item{...}{Arguments forwarded directly to
\code{fireSenseUtils:::fireSenseCovariatesCreate()}, defining the inputs and
options required to construct fuel covariates.}

\item{rasTemplate}{A raster-like object (typically \code{terra::SpatRaster}) used
as a spatial template (extent, resolution, projection) when converting the
fuel covariate data frame to a raster.}

\item{fact}{\code{numeric(1)} aggregation factor passed to
\code{terra::aggregate(fact = ...)}, controlling the spatial coarsening scale.}
}
\value{
A \code{terra::SpatRaster} containing aggregated fuel covariate layers at the
coarser spatial resolution defined by \code{fact}.
}
\description{
Creates fuel covariate layers from tabular inputs using
\code{fireSenseUtils:::fireSenseCovariatesCreate()}, converts the resulting data
frame into a raster using \code{SpaDES.tools::rastFromDF()}, and aggregates the
raster to a coarser spatial resolution with \code{terra::aggregate()} using the
mean.
}
\details{
The function performs the following steps:
\enumerate{
\item Generates a fuel covariate data frame using
\code{fireSenseUtils:::fireSenseCovariatesCreate(...)}.
\item Converts the data frame to a raster aligned with \code{rasTemplate} via
\code{SpaDES.tools::rastFromDF()}.
\item Aggregates the raster to a coarser resolution using
\code{terra::aggregate(fact = fact, fun = mean)}.
}

The aggregation is applied across all raster layers (if present), computing
the mean of contributing cells.
}
\note{
This function uses a non-exported function from \code{fireSenseUtils} via \code{:::},
which may change without notice across package versions.
}
\section{Assumptions and required environment}{

The function assumes that the following are available:
\itemize{
\item \code{fireSenseUtils:::fireSenseCovariatesCreate()} (non-exported API),
\item \code{SpaDES.tools::rastFromDF()},
\item the \code{terra} package.
}

No input validation is performed on the structure of the data returned by
\code{fireSenseCovariatesCreate()}.
}

\examples{
\dontrun{
fuelCovsCoarse <- prepare_FuelCovsCoarse(
  fuels = fuelTable,
  rasTemplate = templateRaster,
  fact = 4
)
}

}
\references{
\itemize{
\item \code{terra} package reference manual: raster aggregation with \code{aggregate()}.
\item \code{SpaDES.tools} package reference manual: \code{rastFromDF()}.
\item \code{fireSenseUtils} package source code: \code{fireSenseCovariatesCreate()} (internal API).
}
}
\seealso{
\code{\link[terra:aggregate]{terra::aggregate()}}, \code{\link[SpaDES.tools:rastFromDF]{SpaDES.tools::rastFromDF()}}
}
\keyword{aggregation}
\keyword{covariates}
\keyword{fuel}
\keyword{raster}
