% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ELFfireData.R
\name{ELFfireCounts}
\alias{ELFfireCounts}
\title{Count fires in each ELF}
\usage{
ELFfireCounts(
  rasWhole,
  firePoints,
  firePolys,
  fireYears,
  pixelAreaHa,
  naturalCauses = c("L", "N")
)
}
\arguments{
\item{rasWhole}{\code{SpatRaster} of ELF layers on one common grid, one layer per ELF, as
built by \code{\link[=makeELFs]{makeELFs()}} (\code{sim$ELFs$rasWhole}). Cells are 0 (outside), 1 (buffer) or
2 (core).}

\item{firePoints}{\code{SpatVector} of fire points, as returned by
\code{fireregimetools::load_nfdb_points()}. Needs columns \code{YEAR} and \code{CAUSE}.}

\item{firePolys}{\code{SpatVector} of fire perimeters, as returned by
\code{fireregimetools::load_nbac_polys()}. Needs column \code{YEAR}.}

\item{fireYears}{integer vector of the years being fitted.}

\item{pixelAreaHa}{area of one fitting pixel, in hectares. A polygon counts when its
area inside the ELF exceeds this.}

\item{naturalCauses}{causes treated as natural ignitions.}
}
\value{
A \code{data.table} with one row per ELF and year: \code{ELF}, \code{year},
\code{naturalIgnitions}, \code{firePolygons}.
}
\description{
Counts natural-cause ignitions and fire polygons falling in each ELF's fitting
area (core plus buffer), per year.
}
\details{
Points are counted on the grid of \code{rasWhole} with a single \code{\link[terra:cellFromXY]{terra::cellFromXY()}}
lookup. Polygons are intersected with each ELF's area and their clipped area is
compared with \code{pixelAreaHa}, mirroring the size filter the spread fit applies
(\code{fireSense_dataPrepFit.R:966-982}).
}
