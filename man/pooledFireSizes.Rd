% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objFunSpread.R
\name{pooledFireSizes}
\alias{pooledFireSizes}
\title{Pool simulated and observed fire sizes over the same years}
\usage{
pooledFireSizes(fireSizesList, yrsDoneList, historicalFiresAboveMin)
}
\arguments{
\item{fireSizesList}{List with one element per batch, each a numeric vector of
simulated fire sizes. Elements for batches that did not run are \code{NULL}.}

\item{yrsDoneList}{List with one element per batch, each the year names
contributing to the matching element of \code{fireSizesList}.}

\item{historicalFiresAboveMin}{Named list of observed fires, one element per
year, each with a \code{size} column.}
}
\value{
A list of two numeric vectors, \code{simulated} and \code{observed}, covering the
same years.
}
\description{
The Anderson-Darling test in \code{\link[=objFunSpread]{objFunSpread()}} compares whole distributions of
fire size, so both samples must be drawn from the \emph{same} set of years. The years
are simulated in batches (largest-area years first, so a hopeless parameter set
can bail early), and this assembles the two samples from the per-batch
accumulators, skipping any batch that did not run.
}
\keyword{internal}
