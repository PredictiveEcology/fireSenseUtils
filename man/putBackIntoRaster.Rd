% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuelClassPrep.R
\name{putBackIntoRaster}
\alias{putBackIntoRaster}
\title{Rebuild land cover layers back into a SpatRastser}
\usage{
putBackIntoRaster(lcc, landcoverDT, flammableMap)
}
\arguments{
\item{lcc}{character vector.
Names of the land cover class columns in \code{landcoverDT} to rebuild into
raster layers (e.g., \code{c("Conifer", "Deciduous", "Shrub")}).}

\item{landcoverDT}{data.table.
A \code{data.table} that must contain:
\itemize{
\item a column \code{pixelID} with integer indices into the template raster's value
vector (as returned by \code{terra::values(flammableMap, mat = FALSE)}), and
\item one numeric column per class named in \code{lcc}.
}}

\item{flammableMap}{SpatRaster.
A \code{terra::SpatRaster} used as the spatial template (geometry, extent,
resolution, and \code{NA} mask) for the output layers.}
}
\value{
A multi-layer \code{terra::SpatRaster} where each layer corresponds to one entry
in \code{lcc}, with layer names set to \code{lcc}.
}
\description{
Given a template raster (e.g., a flammability map) and a \code{data.table}
containing pixel values for one or more land cover classes, this function
reconstructs each class as a raster layer and returns a multi-layer
\code{SpatRaster} whose layer names match the class names supplied in \code{lcc}.
}
\details{
This function:
\enumerate{
\item Creates a working copy of the template raster's values (initialized to 0
on all non-\code{NA} cells and keeping original \code{NA}s).
\item For each class name in \code{lcc}, it pulls the corresponding vector from
\code{landcoverDT}, assigns those values into the
working vector at positions indicated by \code{landcoverDT$pixelID}, and then
writes these values back into a fresh \code{terra::rast(flammableMap)} layer.
\item Stacks all layers into a single \code{terra::rast()} and sets layer names to
\code{lcc}.
}

\strong{Assumptions / requirements}
\itemize{
\item \code{flammableMap} is a \code{terra::SpatRaster} providing the spatial geometry,
extent, resolution, and \code{NA} mask.
\item \code{landcoverDT} is a \code{data.table} with a column named \code{pixelID} that indexes
cell positions in the template raster's value vector (i.e., values returned
by \code{terra::values(flammableMap, mat = FALSE)}). Indices must be valid and
1-based (R-style).
\item Each element of \code{lcc} is the name of a numeric column in \code{landcoverDT}.
\item All layers are created in the same CRS, resolution, and extent as
\code{flammableMap}.
}

\strong{Notes}
\itemize{
\item Non-\code{NA} cells in the template are initialized to 0 before assignment, so
only positions referenced by \code{pixelID} will receive class values; other
non-\code{NA} positions remain 0 in each layer.
\item If you want to preserve existing values outside \code{pixelID}, modify the
initialization step accordingly.
}
}
\examples{
\dontrun{
library(terra)
library(data.table)

# Template raster (5x5 example)
r <- rast(nrows = 5, ncols = 5, vals = NA)
# Mark some cells as valid (non-NA)
values(r) <- rep(NA_real_, ncell(r))
valid_idx <- c(3, 5, 7, 13, 17, 20)
tmp_vals <- values(r, mat = FALSE)
tmp_vals[valid_idx] <- 0
r <- setValues(r, tmp_vals)

# Data table with pixel indices and two classes
dt <- data.table(
  pixelID = valid_idx,
  Conifer  = c(0.6, 0.2, 0.8, 0.1, 0.0, 0.4),
  Deciduous = c(0.1, 0.3, 0.0, 0.4, 0.5, 0.2)
)

# Rebuild layers
out <- putBackIntoRaster(
  lcc = c("Conifer", "Deciduous"),
  landcoverDT = dt,
  flammableMap = r
)

out
names(out)        # "Conifer" "Deciduous"
plot(out)
}

}
\seealso{
\code{\link[terra:rast]{terra::rast()}}, \code{\link[terra:values]{terra::values()}}, \code{\link[terra:setValues]{terra::setValues()}}
}
