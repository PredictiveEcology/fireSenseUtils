% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitDiagnostics.R
\name{fitDiagnostics}
\alias{fitDiagnostics}
\alias{spreadProbSummary}
\alias{combineSpreadProbSummaries}
\alias{simulateFireSizes}
\alias{scoreFireSizes}
\alias{linkSaturation}
\alias{coefIdentifiability}
\alias{profileCoefficients}
\alias{identifiedInIsolation}
\alias{fitConvergence}
\title{Diagnostics of a fitted spread model}
\usage{
spreadProbSummary(p, ceiling, tol = 0.002, binWidth = 0.001)

combineSpreadProbSummaries(x)

simulateFireSizes(
  pop,
  fn = .objfunSpreadFit,
  fnArgs = list(),
  cl = NULL,
  seed = 1L
)

scoreFireSizes(
  sims,
  probs = c(0.01, 0.1, 0.25, 0.5, 0.75, 0.9, 0.99),
  largeFire = 174
)

linkSaturation(sims, probsP = c(0.01, 0.1, 0.25, 0.5, 0.75, 0.9, 0.99))

coefIdentifiability(pop, lower, upper, zMin = 1)

profileCoefficients(
  best,
  pop,
  fn = .objfunSpreadFit,
  reps = 10L,
  cl = NULL,
  seed = 1L,
  fnArgs = list(),
  coefs = covariateCoefs(names(best)),
  probsPop = c(0.05, 0.25, 0.5, 0.75, 0.95)
)

identifiedInIsolation(ident, profile, seMult = 2)

fitConvergence(DE)
}
\arguments{
\item{p}{Numeric. Spread probabilities.}

\item{ceiling}{The link's upper asymptote, \code{maxAsymptote}.}

\item{tol}{A probability within \code{tol} of \code{ceiling} counts as at the ceiling.}

\item{binWidth}{Width of the bins in which \code{p} is counted, so summaries of different years can
be added up and still give quantiles.}

\item{x}{A list of \code{spreadProbSummary()} results with the same \code{binWidth}.}

\item{pop}{A matrix or \code{data.table} of parameter sets, one per row, with column names.}

\item{fn}{The objective; \code{\link[=.objfunSpreadFit]{.objfunSpreadFit()}}.}

\item{fnArgs}{Named list of further arguments to \code{fn}, as for \code{\link[=rescorePopulation]{rescorePopulation()}}. The fit data
(\code{landscape}, \code{annualDTx1000}, ...) are taken from the workers' global environment if absent,
so a held-out prediction passes its own years here.}

\item{cl}{A cluster, or \code{NULL} to run in this session.}

\item{seed}{Every parameter set is evaluated with the same seeds (\code{seed}, or \code{seed + i} for
replicate \code{i} of the profile), so differences between them are not luck.}

\item{sims}{The output of \code{simulateFireSizes()}.}

\item{probs}{Quantiles of fire size to compare.}

\item{largeFire}{Size in pixels above which a fire counts as large (174 pixels = 1000 ha at
5.76 ha per pixel).}

\item{probsP}{Quantiles of spread probability to report.}

\item{lower, upper}{The bounds the fit used, named as the columns of \code{pop}.}

\item{zMin}{The \code{zPop} at or above which the population is taken to pin a coefficient's sign.}

\item{best}{Named numeric: the parameter set to profile around, normally the best member by
replicated mean (\code{\link[=bestByReplicatedMean]{bestByReplicatedMean()}}).}

\item{reps}{Evaluations of each profile point.}

\item{coefs}{The coefficients to profile; all covariate coefficients by default.}

\item{probsPop}{Population quantiles to evaluate each coefficient at, besides 0.}

\item{ident}{The output of \code{coefIdentifiability()}.}

\item{profile}{The output of \code{profileCoefficients()}.}

\item{seMult}{Dropping a coefficient must worsen the objective by more than \code{seMult} standard
errors.}

\item{DE}{The output of \code{\link[=runDEoptim]{runDEoptim()}}: a list of DEoptim results, one per chunk of generations.}
}
\value{
\code{spreadProbSummary()}: a list with \code{n}, \code{atCeiling} (a count), \code{counts} (per bin) and
\code{binWidth}.

\code{combineSpreadProbSummaries()}: their sum, as one summary.

\code{simulateFireSizes()}: a \code{data.table}, one row per parameter set x fire x replicate:
\code{member} (row of \code{pop}), \code{yr}, \code{rep}, \code{initialLocus}, \code{ids}, simulated \code{sim} and observed
\code{size}, in pixels. A year the objective will not simulate even with its gate lifted keeps its
fires with \code{sim} \code{NA}. \code{attr(, "spreadProb")} is a list of \code{spreadProbSummary()}s, one per
member.

\code{scoreFireSizes()}: a one-row \code{data.table}. \code{fireBias} and \code{fireRMSE} are the mean and
root mean square of log10(simulated / observed) per fire (0.3 is a factor of 2); \verb{year*} the
same for annual area burned; \code{yearIn90pct} the share of years whose observed area lies within
the 5-95\% range of simulated years; \code{simsOver10xObs} and \code{simsUnderTenthObs} the share of
simulated fires more than 10 times too large or too small; \code{AD} the Anderson-Darling statistic
between simulated and observed sizes; \verb{qObs_*}/\verb{qSim_*} the quantiles in \code{probs}.

\code{linkSaturation()}: a \code{data.table}, one row per member: \code{pixelYears}, \code{propAtCeiling}
and the spread-probability quantiles \verb{p_*} (bin midpoints).

\code{coefIdentifiability()}: a \code{data.table}, one row per covariate coefficient: the
population median \code{popMedian}; \code{spread90}, the 5-95\% range across members as a share of the
bound width; \code{atBound}, the share of members within 5\% of the width from a bound;
\verb{zPop = |popMedian| / ((q95 - q5) / 3.29)}, the median in units of the population's spread
(a normal's 5-95\% range is 3.29 sd); \code{rSlope}, the correlation across members with
\code{hillSlope1} (the link uses \verb{hillSlope1 * X b}, so the two trade off by construction); and
\code{signPinned = zPop >= zMin}. With three runs each of four ELFs, \code{zPop >= 1} found the
coefficients whose sign every run agreed on with 94\% precision and 44\% recall.

\code{profileCoefficients()}: a \code{data.table}, one row per coefficient x profile point: \code{coef},
\code{at} (its value there; \code{isZero} marks the drop test), \code{mean} of the objective over \code{reps}, and
\code{delta} and \code{deltaSE}: the mean paired difference from \code{best} (same seeds) and its standard
error. A positive \code{delta} means the point fits worse than \code{best}. A reference row
(\code{coef = ""}) holds \code{best} itself.

\code{identifiedInIsolation()}: \code{ident} with \code{dropDelta}, \code{dropDeltaSE}, \code{dropMatters} and
\code{identified} (\code{signPinned & dropMatters}) added.

\code{fitConvergence()}: a \code{data.table}, one row per chunk: \code{generation} (its last),
\code{bestval} (the best value so far) and the median and standard error of the median of the
population's values (fail values of \code{1e6} and above left out).
}
\description{
The checks made by hand on the FireSense phase-2 fits (September 2026), as functions, so that
\code{fireSense_SpreadFit} can make them after every fit.
}
\details{
\itemize{
\item \code{simulateFireSizes()} simulates the observed fires from each of several parameter sets, with
the objective's size cap and "too burny" gate lifted, and returns the simulated sizes.
\item \code{scoreFireSizes()} compares those with the observed sizes: per fire, per year, and by
quantile. It never uses the objective's value.
\item \code{linkSaturation()} reports how many pixel-years sit at the spread-probability ceiling. When
most do, fire size is decided by the ceiling alone ("too many medium, too few large fires").
\item \code{coefIdentifiability()} measures, for each covariate coefficient, how tightly the final
population pins it down.
\item \code{profileCoefficients()} sets each coefficient in turn to 0 and to values across the
population's range, other coefficients held at the best member, and re-scores.
\item \code{identifiedInIsolation()} combines the two: a coefficient is identified in isolation when
the population pins its sign (\code{zPop >= zMin}) and dropping it (setting it to 0) makes the
objective clearly worse.
\item \code{fitConvergence()} summarises the objective across the fit's generations.
}
}
