% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{mergePreparedCovs}
\alias{mergePreparedCovs}
\title{Build ignition covariates table by stacking/extracting rasters and joining lightning}
\usage{
mergePreparedCovs(
  years,
  fuelCovsCoarse,
  ignitionFirePoints,
  nonForestedLCCGroups,
  ignitionClimateCoarse,
  lightningMap,
  digest,
  useCache = TRUE
)
}
\arguments{
\item{years}{A vector or list of year identifiers passed to
\code{\link[=stackAndExtract]{stackAndExtract()}}. Can be a list matching the structure of
\code{fuelCovsCoarse}/\code{ignitionClimateCoarse}.}

\item{fuelCovsCoarse}{A list (possibly nested by year) of named raster-layer
collections (e.g., \code{terra::SpatRaster} or lists of SpatRaster) representing
fuel-related covariates. Names must overlap with LCC group names used to
split forested vs. non-forested classes.}

\item{ignitionFirePoints}{Ignition point locations (e.g., \code{sf}/\code{sp} points or
a \code{data.frame} convertible by \code{stackAndExtract}) used for extraction.}

\item{nonForestedLCCGroups}{A named vector or list whose names identify the LCC
(land-cover) classes considered non-forested. These names are used to split
\code{fuelCovsCoarse} into \code{LCC} (non-forested) and \code{fuel} (forested/other) inputs.}

\item{ignitionClimateCoarse}{A list (possibly by year) of coarse climate rasters
(e.g., \code{terra::SpatRaster}) to include in the stack-and-extract process.
The names of this list are used as \code{userTags} in caching and are later treated
as climate column names when computing row-wise cover sums.}

\item{lightningMap}{A named \code{terra::SpatRaster} (or a named list coercible to one)
containing one or more lightning-derived layers (e.g., \code{"lightningDays"}).
These values are looked up by \code{pixelID} and appended as columns.}

\item{digest}{A cache key supplement (character or list) forwarded to
\code{\link[reproducible:Cache]{reproducible::Cache()}} via \code{.cacheExtra} to ensure cache correctness for
large inputs omitted from the hash.}

\item{useCache}{\code{logical} (default \code{TRUE}). If \code{TRUE}, wrap the Map call in
\code{\link[reproducible:Cache]{reproducible::Cache()}} with \code{omitArgs = c("fuel", "LCC")}, \code{.cacheExtra = digest},
and \code{userTags = names(ignitionClimateCoarse)}.}
}
\value{
A \code{data.table} of ignition covariates with:
\itemize{
\item \code{pixelID} (formerly \code{cell})
\item \code{ignitions}
\item Climate covariate columns (names derived from \code{ignitionClimateCoarse})
\item Cover/class covariates retained from \code{stackAndExtract()} (filtered to rows with non-zero cover sum)
\item Lightning-derived columns appended from \code{lightningMap}
\item \code{year} coerced to numeric
}
}
\description{
For each \code{year} (or year period) provided, stacks and extracts covariates from
coarse fuel rasters (split into forested vs. non-forested LCC groups) and climate,
at ignition point locations, then merges results across years, filters unusable
pixels, appends lightning summaries, and orders columns. Results are optionally
cached.
}
\details{
\strong{Processing steps:}
\enumerate{
\item Calls \code{\link[=stackAndExtract]{stackAndExtract()}} via \code{Map()} for each entry in \code{years},
splitting \code{fuelCovsCoarse} per year into:
\itemize{
\item \code{fuel}: all layers not in \code{names(nonForestedLCCGroups)}
\item \code{LCC}: layers whose names are in \code{names(nonForestedLCCGroups)}
}
Additional \code{MoreArgs} include \code{climate = ignitionClimateCoarse} and
\code{fires = ignitionFirePoints}.
\item Optionally caches the list of per-year extractions with
\code{\link[reproducible:Cache]{reproducible::Cache()}} (function name tagged as \code{"stackAndExtract"}).
\item Combines the list to a single \code{data.table} via \code{\link[data.table:rbindlist]{data.table::rbindlist()}}.
\item Removes rows for which the sum across all cover-type columns is zero
(computed as \code{rowSums(.SD)} over all columns except \code{ignitions},
\code{yearChar} (from \code{fireSenseUtils}), \code{cell}, and the climate variable
names).
\item Renames the \code{cell} column to \code{pixelID}, coerces \code{year} to numeric,
and appends lightning columns by indexing \code{terra::values(terra::rast(lightningMap))}
with \code{pixelID}.
\item Optionally reorders columns to place \code{pixelID}, \code{ignitions},
climate columns (i.e., \code{names(ignitionClimateCoarse)}), and \code{youngAgeName}
(if it exists) first.
}

\strong{Assumptions / Requirements:}
\itemize{
\item \code{fireSenseUtils::stackAndExtract()} must accept arguments \code{years}, \code{fuel}, \code{LCC},
\code{climate}, and \code{fires}, and return a data.table-like object with at least
\code{cell}, \code{ignitions}, and \code{year} (or \code{yearChar}) columns.
\item \code{fireSenseUtils::yearChar} is used as a column name to exclude from cover sums;
ensure it exists/aligns with the data produced by \code{stackAndExtract()}.
\item \code{youngAgeName} is referenced when establishing column order but is \strong{not}
defined in this scope; if unavailable, it is silently omitted.
\item \code{pixelID} is assumed to be a valid row index into \code{lightningMap} raster values.
}

\strong{Lightning layer choice:} The code appends all layers provided in \code{lightningMap}.
If a single best-performing layer is known (e.g., \code{"lightningDays"}), subset
\code{lightningMap} before calling this function.

\strong{Caching:} Large raster inputs are excluded from the cache key via
\code{omitArgs = c("fuel", "LCC")}; the caller-supplied \code{digest} should capture
the salient identity of those omitted objects to avoid cache collisions.
}
\section{Potential pitfalls}{

\itemize{
\item If \code{pixelID} indexes are not aligned with \code{lightningMap} cell indices,
appended lightning values will be incorrect; ensure consistent indexing/resolution.
\item If \code{fireSenseUtils::yearChar} is absent in the returned data, the exclusion in
\code{.SDcols} is harmless, but confirm that the intended year column exists for analysis.
\item If \code{youngAgeName} is not defined, it will be ignored when reordering columns.
}
}

\examples{
\dontrun{
# Pseudocode, assuming you have compatible inputs prepared:
yrs <- list(`2001` = 2001L, `2002` = 2002L)
# fuelCovsCoarse[[year]] is a named list or SpatRaster with LCC class names
# ignitionClimateCoarse[[year]] is a SpatRaster of climate layers
# ignitionFirePoints are point locations (sf/sp/data.frame accepted by stackAndExtract)
# lightningMap is a named SpatRaster (e.g., "lightningDays")

out <- mergePreparedCovs(
  years = yrs,
  fuelCovsCoarse = fuelCovsCoarse,
  ignitionFirePoints = ignitionFirePoints,
  nonForestedLCCGroups = c("Shrub", "Grass", "Bare"),
  ignitionClimateCoarse = ignitionClimateCoarse,
  lightningMap = lightningMap["lightningDays"],
  digest = list(seed = 1L, cfg = "v1"),
  useCache = TRUE
)
data.table::str(out)
}

}
\seealso{
fireSenseUtils::stackAndExtract, reproducible::Cache, data.table::rbindlist,
terra::rast, terra::values
}
