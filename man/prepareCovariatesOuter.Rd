% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{prepareCovariatesOuter}
\alias{prepareCovariatesOuter}
\title{Prepare and (optionally) cache scaled covariates for modeling}
\usage{
prepareCovariatesOuter(unscaledData, algorithm, rescaleVars, useCache = TRUE)
}
\arguments{
\item{unscaledData}{A \code{data.frame} or \code{data.table} of covariates. Must
include predictor columns and may include response columns such as
\code{ignitions} and/or \code{escapes} (see Details).}

\item{algorithm}{\code{character}. Name of the model algorithm. If it contains
\code{"xgb"}, covariates are standardized via \code{\link[base:scale]{base::scale()}} (centering and
scaling); otherwise, covariates are rescaled by order of magnitude to
roughly the [0, 10] range (see Details).}

\item{rescaleVars}{\code{logical}. If \code{TRUE}, perform rescaling/standardization
according to \code{algorithm}; if \code{FALSE}, covariates are returned unchanged.}

\item{useCache}{\code{logical} (default \code{TRUE}). If \code{TRUE}, results are cached
using \code{\link[reproducible:Cache]{reproducible::Cache()}} with an extra key derived from a digest of
\code{unscaledData}.}
}
\value{
A \code{list} with elements:
\describe{
\item{covariates}{A \code{data.table} with rescaled/standardized covariates.}
\item{formula}{\code{NULL} (the non-xgboost formula path is disabled).}
\item{ignitionRescalers}{Named numeric vector of per-variable 10-based
rescalers (non-xgboost path) or \code{NULL} for xgboost / no-rescale.}
\item{xvar}{Character vector indicating which year-like column(s) were found.}
\item{digestOfData}{The digest used for caching, or \code{NULL} if \code{useCache = FALSE}.}
}
}
\description{
Wraps \code{\link[=rescaleCovariates]{rescaleCovariates()}} to rescale features according to the modeling
algorithm, and optionally caches results based on a robust digest of the
unscaled input data. Returns a list containing the (possibly) rescaled
covariates, the modeling formula (if any), per-variable rescalers (if any),
the x-axis variable name(s), and the digest used for caching.
}
\details{
This function:
\itemize{
\item Computes a digest of \code{unscaledData} (if \code{useCache = TRUE}) using an internal
\code{.robustDigest()} to form a \code{.cacheExtra} key for \code{\link[reproducible:Cache]{reproducible::Cache()}}.
\item Calls \code{\link[=rescaleCovariates]{rescaleCovariates()}} with \code{formula = NULL} and \code{family = NULL}
(non-xgboost code-paths were removed).
\item Caches the result while omitting the large arguments \code{"covariates"} and
\code{"formula"} from cache key construction (via \code{omitArgs}), relying on the
explicit digest instead.
}

\strong{Expected columns}:
\itemize{
\item \code{ignitions}: if present, is preserved through standardization (xgboost path).
\item \code{escapes}: if present, is preserved through standardization (xgboost path).
\item \code{year} or \code{yr}: used to infer \code{xvar} (see \code{rescaleCovariates()}).
}
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(pixelID = 1:3,
                 year = c(2001L, 2002L, 2003L),
                 ignitions = c(0, 1, 0),
                 x1 = c(0.01, 2.3, 15))

out <- prepareCovariatesOuter(unscaledData = dt,
                              algorithm = "xgb_classifier",
                              rescaleVars = TRUE,
                              useCache = FALSE)
str(out)
}

}
\seealso{
\code{\link[=rescaleCovariates]{rescaleCovariates()}}, \code{\link[reproducible:Cache]{reproducible::Cache()}}
}
