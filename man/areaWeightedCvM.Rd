% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objFunSpread.R
\name{areaWeightedCvM}
\alias{areaWeightedCvM}
\title{Area-weighted Cramér-von Mises distance between two fire-size samples}
\usage{
areaWeightedCvM(sim, obs)
}
\arguments{
\item{sim, obs}{Numeric vectors of fire sizes (pixels).}
}
\value{
A single non-negative numeric: 0 when the two samples have the same area shares, at most
\code{length(obs)}, which is also what it returns when there are no simulated fires.
}
\description{
Compares the share of total area burned that comes from fires up to each size, \code{F_w(x) = sum(size[size <= x]) / sum(size)}, for the simulated and the observed fires. The squared difference is
averaged over the observed fires, each weighted by its share of the observed area, and multiplied by the
number of observed fires, so it grows with the sample like the AD statistic. It depends only on the order
of sizes, so it is the same on the log scale. Large fires dominate it, as they do area burned.
}
\keyword{internal}
