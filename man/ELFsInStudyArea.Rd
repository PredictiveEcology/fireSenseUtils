% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ELFs.R
\name{ELFsInStudyArea}
\alias{ELFsInStudyArea}
\title{Identify and rasterize Ecologically-based Low Fractal-dimensional objects within a study area}
\usage{
ELFsInStudyArea(studyArea, inputPath, ELFsRaster = NULL, ELFsPolygon = NULL)
}
\arguments{
\item{studyArea}{A \code{SpatVector} (or object coercible to one) defining the
area of interest. Will be reprojected to the CRS of \code{ELFs} internally.}

\item{inputPath}{Character. Directory in which to store any downloads
produced by \code{ELFtemplateRaster()} when \code{ELFsPolygon} is not supplied.}

\item{ELFsRaster}{Optional named list (one entry per ELF) of \code{SpatRaster}
objects containing the integer values \code{0}, \code{1}, and \code{2}, accessed via
\code{ELFsRaster$rasWhole}. If \code{NULL}, the per-ELF rasters must be loaded by
the caller before invoking this function.}

\item{ELFsPolygon}{Optional \code{SpatVector} of ELF polygons. If \code{NULL}
(the default), ELFs are generated by calling \code{makeELFs()} with a 20 km
buffer around \code{rastTemplate}.}
}
\value{
A categorical \code{SpatRaster} with a \code{value}/\code{ELFind} levels table,
where \code{ELFind} gives the ELF identifier (or \code{"none"} for background).
}
\description{
Intersects a study area with a set of Ecologically-based Low
Fractal-dimensional (ELF) polygons, identifies which ELFs overlap the study
area, and constructs a single categorical raster combining each ELF's
per-cell classification. The output raster encodes three priority layers:
cells classified as \code{2} in any ELF (highest priority, labelled by ELF ID),
cells classified as \code{1} in two or more ELFs (shared agreement, labelled by
ELF ID), and a \code{0} background for all other in-domain cells.
}
\details{
The function assumes that \code{ELFs} (a list-like object with \code{rasWhole} rasters
and an \code{ID} field) and \code{rastTemplate} are available in the calling
environment, along with \code{inputPath}. ELF rasters are expected to contain the
integer values \code{0}, \code{1}, and \code{2}, where \code{2} indicates the core area of the ELF
and \code{1} indicates the buffer for that ELF.

The three layers are combined with \code{terra::cover()} so that:
\itemize{
\item \code{2} hits override everything else,
\item \code{1} hits appear only where at least two ELFs agree, and
\item the \code{0} background fills remaining in-domain cells.
}
}
\seealso{
\code{\link[=makeELFs]{makeELFs()}}, \code{\link[terra:classify]{terra::classify()}}, \code{\link[terra:mosaic]{terra::mosaic()}},
\code{\link[terra:cover]{terra::cover()}}
}
