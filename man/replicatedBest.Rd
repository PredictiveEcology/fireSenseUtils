% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicatedBest.R
\name{rescorePopulation}
\alias{rescorePopulation}
\alias{bestByReplicatedMean}
\title{Re-score a population with replication, and pick the best members by their mean}
\usage{
rescorePopulation(pop, fn, reps = 10L, cl = NULL, seed = 1L, fnArgs = list())

bestByReplicatedMean(pop, scores, n = 5L)
}
\arguments{
\item{pop}{A matrix, one parameter set per row, with the parameter names as column names.}

\item{fn}{The objective function; called as \code{fn(par, ...)} with \code{fnArgs}.}

\item{reps}{Evaluations per member.}

\item{cl}{A cluster from \code{\link[parallel:makeCluster]{parallel::makeCluster()}}, or \code{NULL} to evaluate here.}

\item{seed}{Base seed.}

\item{fnArgs}{Named list of further arguments to \code{fn}.}

\item{scores}{The output of \code{rescorePopulation()} for \code{pop}.}

\item{n}{How many members to return.}
}
\value{
\code{rescorePopulation()}: a \code{data.table} with \code{member} (row of \code{pop}), \code{rep} and \code{value}.

\code{bestByReplicatedMean()}: a list with \code{params} (a \code{data.table}, one row per member, best
first), \code{objFunVal} (their replicated means), \code{objFunValSD} and \code{member} (their rows in \code{pop}).
Identical rows of \code{pop} count once, so the members returned are distinct.
}
\description{
DEoptim evaluates each surviving member only once, so on a noisy objective the values it holds are
partly luck: a lucky low draw survives and is reported as "best". Re-scoring the final population
several times and ranking by the mean sees through that. In eight FireSense fits (2026-09-17),
DEoptim's best member ranked 1st to 6th of 60 by its replicated mean, and the ledger's "5 best" were
five copies of that one member.
}
\details{
\code{rescorePopulation()} evaluates every row of \code{pop} \code{reps} times; each evaluation has its own seed
(\code{seed} + its index), so the result is reproducible. With a cluster the evaluations run on its
workers, which must already hold whatever \code{fn} reads from their global environment (as for DEoptim).
}
