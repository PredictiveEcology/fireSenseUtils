% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{fuelLogToLinear}
\alias{fuelLogToLinear}
\title{Fuel biomass from the log scale back to the linear scale}
\usage{
fuelLogToLinear(x)
}
\arguments{
\item{x}{Numeric vector of fuel biomass as returned by \code{\link[=logMinB]{logMinB()}}.}
}
\value{
Biomass on the linear scale; values on the \code{\link[=logMinB]{logMinB()}} floor become 0.
}
\description{
\code{fireSenseCovariatesCreate()} returns fuel-class biomass as \code{\link[=logMinB]{logMinB()}}: logged, with
everything below \code{exp(log(100) - 1)} (36.8) raised to that floor. On that scale the spread
model cannot use the fuel gradient: on ELF 5.3.2, 45\% of pixels sit on the floor and the
treed ones fall in 16\% of the covariate range (rescaled sd 0.05), so the coefficient
estimates little more than treed against treeless. On the linear scale the treed pixels
span twice the range and an absent fuel is exactly 0, so it contributes exactly nothing.
}
\details{
This undoes the log where the spread model needs it, instead of removing it at the source,
because \code{fireSenseCovariatesCreate()} also builds the ignition covariates and its output is
cached for every fitted polygon. Both \code{fireSense_SpreadFit} and \code{fireSense_SpreadPredict}
call this function, so the fit and the prediction cannot drift apart.
}
