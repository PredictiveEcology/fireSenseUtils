% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuelClassPrep.R
\name{calcNonForestYoungAge}
\alias{calcNonForestYoungAge}
\title{Zero-out young non-forest pixels across LCC layers and add a \code{youngAge} mask}
\usage{
calcNonForestYoungAge(landcoverDT, NFTSD, LCCras, cutoffForYoungAge)
}
\arguments{
\item{landcoverDT}{data.table.
A \code{data.table} containing a \code{pixelID} column of integer cell indices
(1-based) and one or more non-forest columns (all columns except
\code{pixelID} are interpreted as non-forest for the row-sum).}

\item{NFTSD}{numeric.
A numeric vector (or similar) that can be indexed by \code{pixelID} to obtain
per-cell age values.}

\item{LCCras}{SpatRaster.
A \code{terra::SpatRaster} (single- or multi-layer) whose cells correspond to
\code{pixelID}. Pixels identified as "young non-forest" are set to 0 in all
layers. A new \code{youngAge} layer is appended.}

\item{cutoffForYoungAge}{numeric(1).
Age threshold; pixels with \code{age < cutoffForYoungAge} and non-forest
presence are flagged as young.}
}
\value{
A \code{terra::SpatRaster} identical to \code{LCCras} but with:
\itemize{
\item all "young non-forest" pixels set to 0 across its existing layers, and
\item an additional layer named \code{youngAge} where flagged pixels are 1, others are
0, and \code{NA}s follow the template mask.
}
}
\description{
Identifies pixels that have \strong{any non-forest presence} (based on the
non-\code{pixelID} columns of \code{landcoverDT}) and whose \strong{age} is \strong{below}
\code{cutoffForYoungAge}. It then sets those pixels to 0 across \strong{all layers} of
\code{LCCras} and appends a new single-layer raster named \code{youngAge} with 1 for
the affected pixels and 0 elsewhere (preserving \code{NA} where present in the
template).
}
\details{
The function proceeds as follows:
\enumerate{
\item Determines the set of non-forest columns as all columns in \code{landcoverDT}
except \code{pixelID}, and computes a per-row sum (\code{sumRows}).
\item Retrieves pixel ages from \code{NFTSD} using \code{pixelID} as indices.
\item Selects pixels where \code{sumRows > 0} \strong{and} \code{age < cutoffForYoungAge};
these pixels are considered "young non-forest".
\item Sets those pixels to 0 in \strong{every layer} of \code{LCCras} using cell indexing.
\item Builds a new single-layer raster with 1 at those pixel locations and 0
elsewhere (respecting \code{NA} from the template), and adds it to \code{LCCras}
as a layer named \code{youngAge}.
}

\strong{Assumptions / requirements}
\itemize{
\item \code{LCCras} is a \code{terra::SpatRaster} (possibly multi-layer). Cell indexing
with \code{LCCras[pixToChange] <- 0} applies across all layers for the indexed
cells.
\item \code{landcoverDT} is a \code{data.table} with:
\itemize{
\item a \code{pixelID} column of integer cell indices (1-based) consistent with
\code{LCCras} cell numbering, and
\item all other columns representing non-forest variables whose row-wise sum
indicates presence (\code{sumRows > 0}).
}
\item \code{NFTSD} is a numeric vector (or similar) indexable by \code{pixelID}, providing
the age (in the same units as \code{cutoffForYoungAge}) for each cell.
\item \code{cutoffForYoungAge} is a scalar numeric threshold; pixels with \code{age < cutoffForYoungAge} are flagged as "young".
}

\strong{Notes}
\itemize{
\item The function modifies \code{LCCras} in place (by reference to the object passed
in) and returns the modified raster with an extra \code{youngAge} layer.
\item \code{NA} cells in \code{LCCras} remain \code{NA} in the created \code{youngAge} layer.
\item The \code{landcoverDT} temporary columns \code{sumRows} and \code{age} are removed prior
to return.
}
}
\examples{
\dontrun{
library(terra)
library(data.table)

# Build a small template raster
r <- rast(nrows = 3, ncols = 3)
values(r) <- c(NA, 0, 0,
               0, 0, NA,
               0, 0, 0)

# Make a 2-layer LCC raster (copy template twice)
LCC <- c(r, r)
names(LCC) <- c("LCC1", "LCC2")

# A data.table with pixel indices and two non-forest columns
dt <- data.table(
  pixelID = c(2, 3, 4, 5, 7, 8, 9),
  NF1 = c(1, 0, 0, 2, 0, 0, 1),
  NF2 = c(0, 1, 0, 0, 0, 3, 0)
)

# Age vector indexed by cell ID (length ncell(LCC))
ages <- seq_len(ncell(LCC)) * 5  # toy ages

out <- calcNonForestYoungAge(
  landcoverDT = dt,
  NFTSD = ages,
  LCCras = LCC,
  cutoffForYoungAge = 15
)

out
names(out)     # "LCC1" "LCC2" "youngAge"
plot(out$youngAge)
}

}
\references{
\itemize{
\item Terra package reference manual and vignettes for \code{SpatRaster} operations
(Hijmans, R.J.).
\item data.table reference: \code{.SD}, \verb{:=}, and efficient grouping/filtering
(Dowle, M., & Srinivasan, A.).
}
}
\seealso{
\itemize{
\item \code{terra::rast()}, \code{terra::values()}, \code{terra::setValues()} for raster creation
and manipulation.
\item \code{data.table} semantics for \code{.SD}, in-place updates (\verb{:=}), and fast
row/column operations.
}
}
