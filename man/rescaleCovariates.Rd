% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{rescaleCovariates}
\alias{rescaleCovariates}
\title{Rescale/standardize covariates for modeling}
\usage{
rescaleCovariates(formula, covariates, rescaleVars, modelAlgorithm)
}
\arguments{
\item{formula}{A model formula or \code{NULL}. Non-xgboost paths would parse and
validate it, but the surrounding code path currently sets \code{formula = NULL}
and disables non-xgboost modeling in the caller.}

\item{covariates}{A \code{data.frame} or \code{data.table} of covariates and possibly
responses (e.g., \code{ignitions}, \code{escapes}).}

\item{rescaleVars}{\code{logical}. If \code{TRUE}, features are rescaled/standardized
according to \code{modelAlgorithm}. If \code{FALSE}, returned unchanged (with
\code{ignitionRescalers = NULL}).}

\item{modelAlgorithm}{\code{character}. If the string contains \code{"xgb"}, apply
\code{\link[base:scale]{base::scale()}} (centering and scaling) to all columns except
\code{yearChar}, \code{ignitions}, and \code{escapes}. Otherwise, log-10 order-of-magnitude
rescaling is applied to all non-excluded covariates whose maxima fall
outside [0, 10].}
}
\value{
A \code{list} with elements:
\describe{
\item{covariates}{A \code{data.table} after rescaling/standardization.}
\item{formula}{The input \code{formula} (often \code{NULL} in current usage).}
\item{ignitionRescalers}{Named numeric vector of per-variable rescalers (non-xgb) or \code{NULL}.}
\item{xvar}{Character vector with detected year-like column(s) or (bug) \code{rows}.}
}
}
\description{
Applies either order-of-magnitude rescaling to approximately confine features
to the [0, 10] range (non-xgboost) or standardization via \code{\link[base:scale]{base::scale()}}
(xgboost path). Preserves key response columns when present.
}
\details{
\strong{Year variable inference}: If any of \code{c("year", "yr")} match column names (case-insensitive),
\code{xvar} is set to the intersection. Otherwise, the code references \code{rows} (a TODO),
which is undefined here. This appears to be a bug or placeholder and will error
unless \code{rows} exists in scope; consider providing a default (e.g., \code{NULL}) or removing.

\strong{Non-xgboost path}:
\itemize{
\item Excludes \code{pixelID}, \code{ignitions}, \code{escapes}, \code{year}, and \code{yearChar} from rescaling.
\item Computes per-variable \code{rescalers <- 10^(floor(log10(abs(max))))} for variables
whose maxima fall outside [0, 10].
\item Calls \code{rescaleVarsByMagnitude(covariates, ignitionRescalers)} (must exist in scope).
}

\strong{xgboost path}:
\itemize{
\item Standardizes numeric columns except \code{yearChar}, \code{ignitions}, and \code{escapes}.
\item Preserves original \code{ignitions} and (if present) \code{escapes} columns.
\item Attaches centering/scaling attributes under \code{attr(covariates, "scaleData")}.
}

\strong{External references}:
\itemize{
\item Uses \code{ignitionsTxt} and \code{escapesTxt} to
refer to the expected response column names. If these are not available,
ensure compatible strings are provided (see exported constants below).
}
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(ignitions = c(0,1,0),
                 escapes = c(0,0,1),
                 year = 2001:2003,
                 x1 = c(0.05, 12, 100),
                 x2 = c(5, 6, 7))

# XGBoost-like standardization
out_xgb <- rescaleCovariates(NULL, dt, rescaleVars = TRUE, modelAlgorithm = "xgb_tree")
str(out_xgb)

# Non-xgboost magnitude rescaling
out_other <- rescaleCovariates(NULL, dt, rescaleVars = TRUE, modelAlgorithm = "glm")
str(out_other)
}

}
\seealso{
\code{\link[base:scale]{base::scale()}}
}
