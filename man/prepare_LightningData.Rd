% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{prepare_LightningData}
\alias{prepare_LightningData}
\title{Prepare and cache lightning rasters from Google Drive sources}
\usage{
prepare_LightningData(rtm, igAggFactor, dPath)
}
\arguments{
\item{rtm}{\code{terra::SpatRaster} or raster-like object used as the target
template (i.e., an alignment/extent/projection reference) for reading
lightning rasters.}

\item{igAggFactor}{\code{numeric(1)} aggregation factor forwarded to
\code{terra::aggregate(fact = ...)}, typically used to coarsen lightning rasters
for ignition modeling.}

\item{dPath}{Character. Destination path forwarded to
\code{reproducible::prepInputs(destinationPath = ...)}; the directory where
downloaded lightning rasters are stored / cached on disk.}
}
\value{
A named list of aggregated lightning rasters (class \code{list} of
\code{terra::SpatRaster}), with names matching:
\code{c("lightningDays", "lightningDensity", "positiveCG", "positiveCGdensity")}.
}
\description{
Downloads, reads, aggregates, and caches multiple lightning-related rasters
(e.g., daily flashes, flash density, positive CG counts) using
\code{reproducible::prepInputs()}, \code{terra::aggregate()}, and \code{reproducible::Cache()}.
The function builds a cache key that combines local processing context
(\code{rasterToMatch}, \code{igAggFactor}, and the lightning reader) with the remote
hashes of the Google Drive files, so cached results are invalidated whenever
either local parameters or upstream files change.
}
\details{
Lightning data is given by the authors of https://www.tandfonline.com/doi/full/10.1080/07055900.2020.1845117
It is currently not hosted by the authors. We therefore host it on Google Drive.

The function maintains an internal mapping of Google Drive file IDs for
four lightning products:
\itemize{
\item \code{lightningDays}
\item \code{lightningDensity}
\item \code{positiveCG}
\item \code{positiveCGdensity}
}

For each product, it:
\enumerate{
\item Resolves the Drive ID to a human URL, obtains remote metadata (hash),
\item Calls \code{reproducible::prepInputs()} with a reading function that should
return a raster aligned to \code{sim$rasterToMatch},
\item Aggregates the raster with \code{terra::aggregate(fact = P(sim)$igAggFactor)},
\item Caches the result with \code{reproducible::Cache()} under a product-specific
function label, seeding the cache with both local and remote digests.
}

A final \code{Cache()} call wraps the list of per-product results to further
stabilize caching across the set.
}
\note{
If \code{prepInputs()} expects a function for \code{fun}, prefer:

\if{html}{\out{<div class="sourceCode">}}\preformatted{fun = function(...) rld(targetFile, to = sim$rasterToMatch)
}\if{html}{\out{</div>}}

instead of calling \code{rld(...)} immediately.
}
\section{Caching and reproducibility}{

The cache key includes:
\itemize{
\item The remote hash of each Google Drive resource (so cloud-side updates bust
the cache),
\item The local context digest of \code{rasterToMatch}, \code{igAggFactor}, and the reader,
via \code{.robustDigest}.
}

This design minimizes recomputation while remaining sensitive to upstream and
local changes.
}

\examples{
\dontrun{
out <- prepare_LightningData(
  rtm = sim$rasterToMatch,
  igAggFactor = P(sim)$igAggFactor,
  rld = readLightningData
)
out$lightningDensity
}

}
\references{
\itemize{
\item \strong{User-provided function source} (this documentation derives from the code
you supplied).
\item Reproducible workflows with the \code{reproducible} package and raster handling
with \code{terra} as described in their package manuals and vignettes.
}
}
\seealso{
\code{\link[reproducible:prepInputs]{reproducible::prepInputs()}}, \code{\link[reproducible:Cache]{reproducible::Cache()}}, \code{\link[terra:aggregate]{terra::aggregate()}}
}
\keyword{caching}
\keyword{data-preparation}
\keyword{lightning}
\keyword{raster}
\keyword{reproducibility}
