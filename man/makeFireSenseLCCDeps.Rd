% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeFireSenseLCC.R
\name{makeFireSenseLCCDeps}
\alias{makeFireSenseLCCDeps}
\title{Functions whose code affects what \code{\link[=makeFireSenseLCC]{makeFireSenseLCC()}} returns}
\usage{
makeFireSenseLCCDeps(lccSource = getOption("fireSense.lccSource", "SCANFI"))
}
\arguments{
\item{lccSource}{\code{"SCANFI"} (default) or \code{"NTEMS"}; the default can be set with
\code{options(fireSense.lccSource = )}. SCANFI land cover
(\code{\link[LandR:prepInputs_SCANFI_LCC_FAO]{LandR::prepInputs_SCANFI_LCC_FAO()}}) has no wetland classes, so they are added from the Canadian
Wetland Inventory Map (\code{LandR::prepInputs_CWIM()}) with \code{LandR::wetlandToLCC()}: wet treed pixels
become 81, other wet flammable pixels 80, as in NTEMS. This matches Biomass_borealDataPrep's default
land cover. \code{"NTEMS"} uses \code{\link[LandR:prepInputs_NTEMS_LCC_FAO]{LandR::prepInputs_NTEMS_LCC_FAO()}}, which has 80 and 81 already, exactly
as before.}
}
\value{
A list of functions, to pass as \code{reproducible::Cache()}'s \code{.cacheExtra}.
}
\description{
\code{reproducible::Cache()} digests only the called function's own code, so a cached
\code{\link[=makeFireSenseLCC]{makeFireSenseLCC()}} call keeps returning its old result when a function it \emph{calls} changes.
Pass this list as that call's \code{.cacheExtra} and those functions become part of the key.
}
\details{
Ask for the list rather than writing one out: which functions are in the path depends on
\code{lccSource}, which is a run-time option, so a hard-coded list is wrong for the other source and
goes stale when the default moves. It already did -- \code{fireSense_dataPrepFit} pinned
\code{LandR::prepInputs_NTEMS_LCC_FAO()} and kept it after the default became SCANFI, leaving every
SCANFI and CWIM change invisible to the cache.
}
\examples{
\dontrun{
  Cache(makeFireSenseLCC(neededYear = 2020, to = rtm, destinationPath = dPath),
        .cacheExtra = makeFireSenseLCCDeps())
}
}
\seealso{
\code{\link[=makeFireSenseLCC]{makeFireSenseLCC()}}
}
