% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assertions.R
\name{assertCovariateRange}
\alias{assertCovariateRange}
\title{Assert that rescaled spread covariates are usable}
\usage{
assertCovariateRange(dt, colsToUse)
}
\arguments{
\item{dt}{A \code{data.table} of rescaled covariates.}

\item{colsToUse}{Character vector of covariate column names to check.}
}
\value{
\code{invisible(TRUE)}, or an error.
}
\description{
Covariates were historically required to lie in \verb{[0, 1]}. That upper bound was a
convention of the log-biomass parameterisation, not a requirement of the objective
function: covariates enter only through \code{exp(mat \%*\% covPars)} inside \code{\link[=logistic3p]{logistic3p()}},
which saturates gracefully (\code{exp(Inf)^(-b)} is \code{0}, giving \code{maxAsymptote}) and never
produces \code{NaN}. Fuel covariates expressed as \code{biomass / 1e4} legitimately exceed 1,
so only non-negativity and finiteness are enforced.
}
