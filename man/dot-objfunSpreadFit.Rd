% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objFunSpread.R
\name{.objfunSpreadFit}
\alias{.objfunSpreadFit}
\title{Objective function for \code{fireSense_spreadFit} module}
\usage{
.objfunSpreadFit(
  par,
  landscape,
  annualDTx1000,
  nonAnnualDTx1000,
  formulaToFit,
  historicalFires,
  fireBufferedListDT,
  covMinMax = NULL,
  maxFireSpread = 0.28,
  lowerSpreadProb = 0.13,
  minFireSize = 2,
  escapeSizeHa = NULL,
  tests = "snll_fs",
  Nreps = 10,
  mutuallyExclusive = list(youngAge = c("class", "nf")),
  doAssertions = TRUE,
  plot.it = FALSE,
  objFunCoresInternal = 1,
  lanscape1stQuantileThresh = 0.265,
  thresh = 550,
  pruneAbove = Inf,
  covCentre = NULL,
  weighted = TRUE,
  sizeLik = "kde",
  sizeLikDf = 5,
  adWeight = "auto",
  link = NULL,
  returnSims = FALSE,
  capSizes = TRUE,
  fitYearSpreadSD = NULL,
  verbose = 2,
  ...
)
}
\arguments{
\item{par}{parameters}

\item{landscape}{A \code{SpatRaster} with extent, resolution, and projection (crs) used for
\link[SpaDES.tools:spread2]{SpaDES.tools::spread2}.}

\item{annualDTx1000}{A list of data.table class objects. Each list element is
data from a single calendar year, and whose name takes the form \code{"yearxxxx"},
where \code{xxxx} is the four-digit year. The columns in the data.table must integers,
that are \verb{1000x} their actual values as this function will divide by 1000.}

\item{nonAnnualDTx1000}{Like \code{annualDTx1000}, but with where each list element will be
used for >1 year. The names of the list elements must be of the form
\code{"yearxxxx_yearyyyy_yearzzzz"} where \code{xxxx}, \code{yyyy}, and \code{zzzz} represent the four-digit
calendar years for which that list element should be used.
The columns are variables that are used for more than 1 year.}

\item{formulaToFit}{Formula, put provided as a character string, not class \code{formula}.
(if it is provided as a class \code{formula}, then it invariably will have an
enormous amount of data hidden in the formula environment; this is bad for \link[DEoptim:DEoptim]{DEoptim::DEoptim})}

\item{historicalFires}{A named list of \code{data.frame}s (one per year, names
matching those of \code{annualDTx1000}), each with columns \code{cells} (pixel
indices of ignitions) and \code{size} (fire size in pixels). The observed
fire record against which the simulation is compared.}

\item{fireBufferedListDT}{A named list of \code{data.table}s (one per year,
names matching \code{annualDTx1000}), each with columns \code{pixelID}, \code{buffer}
(\code{1} if the pixel is inside a buffered fire footprint, \code{0} otherwise),
and \code{ids} (a unique identifier per fire). Restricts the set of pixels
used when fitting and scoring spread.}

\item{covMinMax}{This is a 2 row by multiple column data.frame indicating
the minimum and maximum values of the original covariate data values.
These will be used to rescale the covariates internally so that they are all between 0 and 1.
It is important to not simply rescale internally here because only 1 year is run at a time;
all years must be rescaled for a given covariate by the same amount.}

\item{maxFireSpread}{A value for \code{spreadProb} that is considered impossible to go above.
Default 0.28, which is overly generous unless there are many non-flammable pixels (e.g., lakes).}

\item{lowerSpreadProb}{Numeric. Lower bound for \code{spreadProb}; if a candidate
\code{spreadProb} falls at or below this, the call exits early to avoid wasted
simulation work. Default \code{0.13}.}

\item{minFireSize}{Integer. Minimum fire size (in pixels) to include when
scoring simulated against historical fires; fires smaller than this are
filtered out of the comparison. Default \code{1}.}

\item{escapeSizeHa}{Numeric or \code{NULL}. Size (ha) a fire must reach to count as escaped. \code{NULL} (the
default) keeps the historical rule: observed fires of at least \code{minFireSize} pixels, simulated fires
of more than 1 pixel in the size likelihood, and every simulated fire in the AD test. When set, the
observed fires AND the simulated fires in both tests are limited to fires of at least
\code{\link[=escapeSizePixels]{escapeSizePixels()}} pixels, so the spread model is fitted as "given the fire escaped".}

\item{tests}{One or more of \code{"mad"}, \code{"adTest"}, \code{"SNLL"}, or \code{"snll_fs"}. Default: \code{"snll_fs"}.}

\item{Nreps}{Integer. The number of replicates, per ignition, to run.}

\item{mutuallyExclusive}{Named list of vectors describing groups of model
terms that must not be active together (mutually exclusive in the
formula). Default \code{list("youngAge" = c("class", "nf"))}.}

\item{doAssertions}{Logical. If \code{TRUE}, the default, the function will test a few minor things
for consistency. This should be set to \code{FALSE} for operational situations, as the assertions
take some small amount of time.}

\item{plot.it}{Passed to \code{\link[SpaDES.core:Plots]{SpaDES.core::Plots()}}, so will show (TRUE or "screen") or save files
of several plots, using \pkg{ggplot2}.}

\item{objFunCoresInternal}{Internally, this function can use \code{mcmapply} to run multiple
parallel \code{spread} function calls. This should only be > \code{1L} if there are spare threads.
It is highly likely that there won't be. However, sometimes the \link[DEoptim:DEoptim]{DEoptim::DEoptim} is
particularly inefficient, it starts X cores, and immediately several of them are
stopped inside this function because the parameters are so bad, only two years are attempted.
Then the core will stay idle until all other cores for the \link[DEoptim:DEoptim]{DEoptim::DEoptim} iteration are complete.
Similarly, if only physical cores are used for \link[DEoptim:DEoptim]{DEoptim::DEoptim}, the additional use of
hyperthreaded cores here, internally will speed things up (i.e., this maybe could be \code{2L} or \code{3L}).}

\item{lanscape1stQuantileThresh}{A \code{spreadProb} value that represents a threshold for the
1st quantile of the \code{spreadProbs} on the landscape; if that quantile is above this
number, then the \code{.objFunSpredFit} will bail because it is "too burny" a landscape.
Default 0.265, meaning if only 25\%\% of the pixels on the landscape are below
this \code{spreadProb}, then it will bail.}

\item{thresh}{Threshold multiplier used in SNLL fire size (\code{"snll_fs"}) test. Default 550.}

\item{pruneAbove}{Numeric. An upper bound on the accumulated SNLL after the \emph{first} batch of
fire years, past which the evaluation stops early and returns the fail value. Default \code{Inf},
which leaves \verb{thresh * <years done>} as the only bound -- i.e. no change in behaviour.

A caller that runs one DEoptim generation per call knows the current population, and can pass
the worst objective value that population would accept. Doing so is \emph{exact}, not a heuristic:
the second batch contributes a non-negative SNLL, so the first batch's accumulated value is a
lower bound on the evaluation's final value. A trial exceeding \code{pruneAbove} would therefore
have finished at or above a value every parent already beats, and selection would have
discarded it. Pruning cannot change which trials DEoptim keeps; it only avoids finishing an
evaluation whose outcome is already decided. This matters because a generation is
synchronous -- its wall time is the slowest of its \code{NP} evaluations, not the median one.
Lowering the threshold value will be more restrictive, but being too restrictive will result
in \link[DEoptim:DEoptim]{DEoptim::DEoptim} rejecting more tests and using the "fail value" of 10000.
Too high a threshold, and more years will be run and it will take longer to find values.}

\item{covCentre}{Optional named list or vector of values subtracted from the rescaled covariates
of the same name. \code{NULL} (default) does no centring. Must be the same at fit and predict.}

\item{weighted}{Should each fire's log-likelihood be multiplied by a weight that grows with its
observed size, to give large fires more influence on the SNLL? \code{FALSE}: no. \code{TRUE} or \code{"log"}:
\code{log(size)}. \code{"sqrt"}: \code{sqrt(size)}. Weights are divided by their mean over the fitted fires, so
the SNLL keeps its scale against the other tests.}

\item{sizeLik}{How a fire's likelihood is taken from its \code{Nreps} simulated sizes, on the
square-root scale. \code{"kde"} (default): their kernel density at the observed size
(\code{\link[EnvStats:demp]{EnvStats::demp()}}); it is zero away from the simulated sizes, so a fire they never reach
scores the \code{minLik} floor however far off it is. \code{"t"}: a Student-t centred on their mean with
their standard deviation as scale; no floor, and a miss costs more the further it is.}

\item{sizeLikDf}{Degrees of freedom of the Student-t when \code{sizeLik = "t"}. Default 5. Lower
values forgive a missed fire more.}

\item{adWeight}{What the \code{"adTest"} statistic is multiplied by before it is added to the fire-size
SNLL. \code{"auto"} (default) uses \code{\link[=adWeightAuto]{adWeightAuto()}}, which gives the two terms about equal influence
for any \code{sizeLik} and \code{weighted}. A number is used as given; the former fixed default, \code{50},
balanced the two only for \code{sizeLik = "kde"} with \code{weighted = FALSE}.}

\item{link}{Passed to \code{\link[=logisticAll]{logisticAll()}}: \code{NULL} for the form implied by the logistic parameters, or
\code{"logistic3pUpper"} for the 3-parameter logistic with Stukel's upper tail, whose fourth logistic
parameter is \code{upperTail1}. Pass it explicitly when fitting: \code{par} may arrive unnamed.}

\item{returnSims}{If \code{TRUE}, return the simulated fires instead of the objective value: a
\code{data.table} with one row per fire year x fire x replicate -- \code{yr}, \code{rep}, \code{initialLocus}, \code{ids},
the simulated size \code{sim} and the observed \code{size}, in pixels. A year the objective declines to
simulate (its spread probabilities fail the sanity checks) keeps its fires with \code{rep} and \code{sim}
\code{NA}, so the caller can count them. Every year is simulated: the early stop (\code{thresh}) is not
applied. For diagnostics and validation; the value is not an objective.
The result carries \code{attr(, "spreadProb")}: the spread probabilities of every fitted pixel-year,
summarised by \code{\link[=spreadProbSummary]{spreadProbSummary()}}, for \code{\link[=linkSaturation]{linkSaturation()}}.}

\item{capSizes}{If \code{FALSE}, simulated fires are not capped at \code{\link[=multiplier]{multiplier()}} of their observed
size. The fit needs the cap; validation should not have it, or a model that predicts fires far
too large cannot show it.}

\item{fitYearSpreadSD}{If \code{TRUE}, the last element of \code{par} is the sd of a per-year random effect on
the logit of spread probability, a seasonal departure: in each replicate the year draws one
\code{eps ~ N(0, sd^2)} and every pixel of that year spreads with \code{plogis(qlogis(p) + eps)}, so all of the
year's fires are bigger or smaller together. Without it the simulated fire sizes were too alike:
medians too large and the largest fires too small at once (2026-09-23). \code{sd = 0} is the model
without it, exactly (no random numbers are drawn). \code{NULL} (default): \code{TRUE} when \code{par} is named and
includes \code{yearSpreadSD}. DEoptim passes \code{par} unnamed, so \code{runDEoptim()} sets it explicitly.}

\item{verbose}{If >= 2, then this will show more information about \code{spreadProb} fitting.}

\item{...}{This is not used here, but allows for extraneous arguments to not break this function.}
}
\value{
Attempting a weighted likelihood,
\url{https://stats.stackexchange.com/questions/267464/algorithms-for-weighted-maximum-likelihood-parameter-estimation}.
With \code{log(fireSize) * likelihood} for each fire.
}
\description{
Objective function for \code{fireSense_spreadFit} module
}
