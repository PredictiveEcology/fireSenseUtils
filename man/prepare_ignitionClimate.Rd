% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepareIgnitionData.R
\name{prepare_ignitionClimate}
\alias{prepare_ignitionClimate}
\title{Aggregate and cache ignition-related climate rasters}
\usage{
prepare_ignitionClimate(
  ignitionClimateList,
  fact,
  digest = NULL,
  useCache = TRUE
)
}
\arguments{
\item{ignitionClimateList}{A named or unnamed \code{list} of raster-like objects
(typically \code{terra::SpatRaster}) representing historical climate variables
used in ignition modeling.}

\item{fact}{\code{numeric(1)} aggregation factor passed to
\code{terra::aggregate(fact = ...)}.}

\item{digest}{A digest object (e.g., \code{list}, atomic vector) representing
upstream processing context, which will be appended to the input-derived
digest when constructing the cache key.}

\item{useCache}{\code{logical} (default \code{TRUE}). If \code{TRUE}, wrap the per-element
aggregation in \code{reproducible::Cache()} so that re-running with the same
inputs returns the cached result. If \code{FALSE}, force recomputation.}
}
\value{
A \code{list} of aggregated climate rasters (typically \code{terra::SpatRaster}), in
one-to-one correspondence with \code{ignitionClimateList}.
}
\description{
Aggregates a list of ignition-relevant climate rasters to a coarser spatial
resolution using \code{terra::aggregate()} (mean across source cells) and caches
the resulting list with \code{reproducible::Cache()}. The cache key is augmented
with a robust digest of the input rasters and a caller-provided upstream
digest.
}
\details{
The function:
\enumerate{
\item Computes a robust digest of \code{ignitionClimateList} using
\code{.robustDigest()} to capture changes in input rasters.
\item Appends that digest to the user-supplied \code{digest} to form a composite
cache key (\code{dig3}).
\item Aggregates each raster via \code{terra::aggregate(fact = fact, fun = mean)}.
\item Wraps the aggregated list in \code{reproducible::Cache()} with
\code{omitArgs = "x"} (to exclude the ephemeral list element passed to
\code{lapply}) and \code{.cacheExtra = dig3} to drive cache invalidation from
the composite digest.
}

All rasters are processed independently via \code{lapply()}, preserving list order
and names.
}
\section{Caching and reproducibility}{

Cache invalidation is controlled by \code{.cacheExtra = dig3}, which combines:
\itemize{
\item a robust digest of the input rasters (\code{.robustDigest(ignitionClimateList)}), and
\item the caller-provided \code{digest} (e.g., pipeline/module context).
}
}

\section{Assumptions and required environment}{

The symbols \code{.robustDigest()} and \code{reproducible::Cache()} must be available.
No validation is performed on input types. Aggregation is controlled by the \code{fact} parameter.
}

\examples{
\dontrun{
# Example (requires `terra` rasters and `reproducible` cache setup):
upstreamDigest <- .robustDigest(list(module = "Fire", version = "1.0.0"))
ignitionClimateCoarse <- prepare_ignitionClimate(
  ignitionClimateList = sim$historicalClimateRasters[
    sim$climateVariablesForFire$ignition
  ],
  fact = 4,
  digest = upstreamDigest
)
}

}
\references{
\itemize{
\item \code{terra} package reference manual: \code{aggregate()} (spatial raster aggregation).
\item \code{reproducible} package reference manual and vignettes: \code{Cache()} and \code{.cacheExtra}.
}
}
\seealso{
\code{\link[terra:aggregate]{terra::aggregate()}}, \code{\link[reproducible:Cache]{reproducible::Cache()}}
}
\keyword{aggregation}
\keyword{caching}
\keyword{climate}
\keyword{ignition}
\keyword{raster}
\keyword{reproducibility}
