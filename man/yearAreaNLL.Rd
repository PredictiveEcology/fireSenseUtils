% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objFunSpread.R
\name{yearAreaNLL}
\alias{yearAreaNLL}
\title{Negative log-likelihood of each year's observed area burned}
\usage{
yearAreaNLL(obsYear, simYear, sizeLik = "t", sizeLikDf = 5, minLik = 1e-29)
}
\arguments{
\item{obsYear}{Named numeric: observed total area burned per fit year, in pixels.}

\item{simYear}{Named list, per year: the simulated totals, one per replicate.}

\item{sizeLik, sizeLikDf}{As for \code{.objfunSpreadFit()}.}

\item{minLik}{Floor on each year's likelihood.}
}
\value{
A single numeric: the summed negative log-likelihood over the years of \code{obsYear}.
}
\description{
Scores each fit year's observed total area burned against that year's simulated totals, one per
replicate, with the size likelihood the per-fire term uses (\code{sizeLik}): a Student-t on the square-root
scale (\code{sizeLikT()}) or the empirical kernel density of \code{sqrt(sims)}. A year with fewer than 2 simulated
totals (the objective refused to simulate it) gets \code{minLik}, as its fires do in the per-fire term.
}
\keyword{internal}
